spring:
  cloud:
    gateway:
      discovery.locator.enabled: true
      routes:
        - id: main_service_route
          uri: lb://main-service
          predicates:
            - Path=/categories/**,/admin/categories/**,/compilations/**,/admin/compilations/**,/admin/events/**,/users/{userId}/events/**,/events/**,/admin/locations/**,/locations/**,/users/{userId}/requests/**

        - id: user_service_route
          uri: lb://user-service
          predicates:
            - Path=/admin/users/**

server:
  port: 8080

logging:
  level:
    org:
      springframework:
        cloud:
          gateway: DEBUG